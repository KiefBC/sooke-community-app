import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kiefbc.sookecommunity',
  appName: 'Sooke Community App',
  webDir: 'build'
};

export default config;
